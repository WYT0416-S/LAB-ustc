# 实验四：频繁项集与关联规则挖掘 实验报告

> 中国科大 2026 春 "数据分析及实践" 课程

---

## 一、数据预处理（Task 1）

### 1a. 数据读取与 status 列构建

数据来自 ICLR 2026 的投稿记录（`iclr_2026_data.jsonl`），总共 19,814 篇论文。读进来后先构建了一个 `status` 列来标记录用状态：

- **已接收 / 条件接收**（`Accept` / `Conditional Accept`） → `1`
- **已拒稿**（`Reject`） → `0`
- **撤稿 / 桌拒**（字段为空或其它） → `NaN`

分布如下：5,359 篇接收，8,816 篇拒稿，还有 5,639 篇是撤稿或桌拒（这部分后面会丢掉）。

### 1b. 数据清洗与统计

丢掉所有 status 为空的记录，只保留 `keywords` 和 `status` 两列。清洗后剩下 **14,175 篇**有效论文，其中接收 5,359 篇，整体录用率 **37.81%**。

### 1c. 关键词清洗与候选关键词筛选

这一步做了几件事：

1. **基本清洗**：所有关键词转小写、去掉首尾空格、按分号拆开
2. **同义词合并**：按照实验文档给的映射表，把 LLM/llms、GNN/gnns、diffusion/diffusion model 等不同写法统一成标准形式（共 25 条映射规则）
3. **去重**：同一篇论文里重复的关键词只保留一次

清洗后一共有 20,722 个不重复关键词。接着用 `Counter` 统计词频，取了 Top-20 作为候选关键词：

| 排名 | 关键词 | 出现次数 |
|------|--------|----------|
| 1 | large language models | 2,504 |
| 2 | reinforcement learning | 976 |
| 3 | diffusion models | 699 |
| 4 | benchmark | 389 |
| 5 | reasoning | 333 |
| 6 | representation learning | 281 |
| 7 | generative models | 267 |
| 8 | transformers | 264 |
| 9 | graph neural networks | 254 |
| 10 | interpretability | 249 |
| 11 | deep learning | 243 |
| 12 | multimodal large language models | 204 |
| 13 | vision-language models | 181 |
| 14 | language models | 177 |
| 15 | federated learning | 168 |
| 16 | flow matching | 166 |
| 17 | time series analysis | 147 |
| 18 | optimization | 146 |
| 19 | evaluation | 142 |
| 20 | continual learning | 138 |

可以看到 LLM 相关的词出现了 2,504 次，断崖式领先。RL 排第二 976 次，diffusion 排第三 699 次，这几个基本统治了 ICLR 2026 的热门方向。

最后，只保留**至少包含 1 个候选关键词**的论文，得到 **6,420 篇**论文，其中接收 2,408 篇，录用率 **37.51%**，和筛选前的 37.81% 非常接近，说明这个筛选操作基本没有引入偏差。

### 1d. 事务构建与项集索引字典

为每篇论文构建了一个"购物篮事务"——把论文包含的候选关键词和录用状态打包成一个集合。比如一篇被拒的 LLM 论文，事务就是 `{large language models, reject}`。

同时建立了索引字典 `ind2val`，0~19 对应 20 个候选关键词，20 对应 `accept`，21 对应 `reject`。后续 Apriori 算法都在事务索引上跑。

---

## 二、频繁项集挖掘（Task 2）

### 2a. Apriori 算法实现与挖掘

手写了一个 Apriori 算法，流程是经典的"逐层搜索 + 剪枝"：

1. 扫描事务，统计每个项的频次，筛出 1-频繁项集
2. 由 k-频繁项集自连接生成 (k+1)-候选集，利用"所有子集必须是频繁的"做剪枝
3. 再次扫描事务，筛出 (k+1)-频繁项集
4. 重复直到没有新的频繁项集产生

分别用 min_support = 0.02 和 0.05 跑了两组实验。

**min_support = 0.02（约 128 篇）时的结果：**

- 频繁 1-项集：22 个（所有候选关键词 + accept + reject 全部频繁）
- 频繁 2-项集：18 个，主要是 "大关键词 + accept/reject" 的组合，比如 `{LLM, reject}` 支持度高达 0.2466
- 频繁 3-项集：**只有 1 个**——`{large language models, reinforcement learning, reject}`，支持度 0.0206

**min_support = 0.05（约 321 篇）时的结果：**

- 频繁 1-项集：只剩 7 个——前 5 大关键词 + accept + reject
- 频繁 2-项集：5 个，几乎都是 LLM/RL/diffusion 搭配录用状态
- 频繁 3-项集：**一个都没了**

显然，直接用 20 个候选关键词做挖掘，项的粒度太细、太分散，稍微提高阈值高层项集就全军覆没。

### 2b. 关键词主题合并后挖掘

为了解决 2a 中数据稀疏的问题，把 20 个候选关键词按实验文档的表格合并成 6 个主题类别：

| 主题类别 | 包含的关键词 |
|----------|-------------|
| **LLM** | large language models, language models, reasoning |
| **MLLM** | multimodal large language models, vision-language models |
| **Generative** | diffusion models, generative models, flow matching |
| **RL** | reinforcement learning |
| **Evaluation** | benchmark, evaluation, interpretability |
| **ML_Foundation** | representation learning, deep learning, transformers, graph neural networks, optimization, federated learning, time series analysis, continual learning |

（注：实验文档的表格列出了 6 个主题类别，第 20 个关键词 "continual learning" 未在表中出现，这里按语义归入了 ML_Foundation。）

同一篇论文里映射到同一主题的多个关键词会去重，比如一篇论文同时有 "transformers" 和 "deep learning"，事务里只保留一个 `ML_Foundation`。

以 min_support = 0.02 重新挖掘：

- 频繁 1-项集：8 个（6 个主题 + accept + reject）
- 频繁 2-项集：**15 个**，主题间的关联开始浮现，比如 `{LLM, RL}` (0.0447)、`{LLM, Evaluation}` (0.0360)
- 频繁 3-项集：**3 个**：
  - `{LLM, RL, accept}`（支持度 0.0215）——LLM+RL 组合且被录用
  - `{LLM, RL, reject}`（支持度 0.0232）——LLM+RL 组合但被拒
  - `{LLM, Evaluation, reject}`（支持度 0.0231）——LLM+评测类被拒

### 2c. 对比分析

两轮挖掘的差异非常明显：

| | Task 2a（20 关键词） | Task 2b（6 主题） |
|---|---|---|
| 频繁 1-项集 | 22 个 | 8 个 |
| 频繁 2-项集 | 18 个 | 15 个 |
| 频繁 3-项集 | 1 个 | 3 个 |

直觉上可能会觉得"关键词数量少了，频繁项集也应该减少"，但实际上 2-项集和 3-项集反而更丰富了。原因其实很简单：**关键词粒度过细导致数据稀疏**。20 个关键词分布在 6,420 篇论文里，大多数关键词的支持度很低（第 10 名以后就不到 4% 了），两个低支持度关键词的组合就更难跨过最小支持度的门槛了。

合并成 6 个主题后，每个主题覆盖了多篇论文，LLM 主题的支持度直接飙到 43.5%，ML_Foundation 也有 23.6%。支持度上去了，高层项集才有机会冒出来。这本质上就是通过**语义聚合**来缓解高维事务数据的稀疏性问题。

---

## 三、关联规则提取（Task 3）

### 3a. 提取 X → {accept} 规则

基于 Task 2b 的频繁项集，用最小置信度 0.4 筛选形如 `X → {accept}` 的规则。每条规则计算了三项指标：

- **支持度**：前件和录用同时出现的比例
- **置信度**：P(录用 | 前件)，即含该主题的论文被录用的概率
- **提升度**：置信度 / 整体录用率，>1 表示该主题对录用有正向影响

共提取到 4 条规则，按提升度降序排列：

| 前件 X | 支持度 | 置信度 | 提升度 |
|--------|--------|--------|--------|
| {LLM, RL} | 0.0215 | 0.4808 | **1.2820** |
| {MLLM} | 0.0259 | 0.4323 | **1.1525** |
| {Generative} | 0.0668 | 0.4218 | **1.1246** |
| {RL} | 0.0634 | 0.4170 | **1.1118** |

四条规则的提升度全部大于 1，说明这些主题方向确实和更高的录用率挂钩。

### 3b. 结果分析

先看各主题的录用率对比：

| 主题 | 论文数 | 录用率 | vs 整体(37.51%) |
|------|--------|--------|-----------------|
| MLLM | 384 | **43.23%** | +5.72% |
| Generative | 1,017 | **42.18%** | +4.67% |
| RL | 976 | **41.70%** | +4.19% |
| Evaluation | 738 | 39.43% | +1.92% |
| LLM | 2,795 | 37.75% | +0.24% |
| ML_Foundation | 1,518 | **31.49%** | -6.02% |

几个有意思的发现：

1. **MLLM 是个香饽饽**：虽然论文数量只有 384 篇（占比很小），录用率却是最高的 43.23%，提升度也排第二。多模态+视觉语言模型确实是当下热点方向。

2. **Generative 和 RL 也很吃香**：录用率都在 42% 左右，比整体高出近 5 个百分点。生成模型和强化学习作为 ICLR 的传统强项，审稿人显然比较买账。

3. **LLM 非常"卷"**：LLM 论文占比高达 43.5%，但录用率只有 37.75%，基本就是平均水平。量大但竞争激烈，单靠"我做 LLM"已经不够了。不过 LLM+RL 的组合（提升度 1.28）是个亮点，跨界组合更容易被录用。

4. **ML_Foundation 最惨**：录用率只有 31.49%，比整体低 6 个点。这个类别包含了 representation learning、deep learning、optimization 等偏基础的方向。可能的原因是这些方向门槛较低、投稿量大、创新点不够突出，审稿人容易审美疲劳。

5. **关于提升度**：四条规则的提升度都在 1.11~1.28 之间，没有特别夸张的数值（比如 >2），说明虽然这些主题对录用有正向影响，但不是决定性因素——论文能不能中，关键还是看具体工作质量而不是选题方向。

---

## 四、总结

这次实验走通了一个完整的关联规则挖掘 pipeline：从 JSONL 数据清洗、关键词标准化、事务构建，到手写 Apriori 挖掘频繁项集，再到提取关联规则并分析提升度。

最核心的收获有两个：一是**数据预处理的质量直接决定挖掘结果的好坏**——同义词不合并干净、关键词不统一，后续分析全白搭；二是**面对高维稀疏数据时，适当的语义聚合（主题归类）能显著提升挖掘的效果**，用 20 个细粒度关键词几乎挖不出高层频繁项集，但合并成 6 个主题后就能看到有意义的模式。

从实际意义上看，分析结果也给出了一些"投稿 tips"：MLLM、Generative、RL 方向在 ICLR 2026 相对好中，LLM 虽然最热门但竞争最激烈，如果做 LLM 的话最好跟 RL 等方向交叉一下，纯 ML 基础方向的论文可能需要更扎实的实验和更突出的创新点才能打动审稿人。
